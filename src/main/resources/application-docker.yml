spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/lgdms
    username: lgdms_user
    password: SecurePass123
  redis:
    host: redis
    port: 6379

logging:
  file:
    name: /app/logs/lgdms.log
